---
phase: 07-goal-directed-behavior
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - goal_manager.py
autonomous: true
requirements:
  - GOAL-01
  - GOAL-05
  - GOAL-06

must_haves:
  truths:
    - "Goals can be created with name, description, status, and subgoals"
    - "Goals persist to goals.json and load at startup"
    - "Completed goals older than 20 are pruned"
  artifacts:
    - path: "goal_manager.py"
      provides: "GoalManager class with CRUD, persistence, and event callbacks"
      exports: ["GoalManager", "Goal", "GoalStatus"]
    - path: "goals.json"
      provides: "Cross-session goal persistence"
  key_links:
    - from: "goal_manager.py"
      to: "context_manager.py"
      via: "GoalManager imports Goal dataclass"
      pattern: "from goal_manager import"
---

<objective>
Create goal model and manager with full CRUD operations and JSON persistence to goals.json.

Purpose: Establish the goal data model (name, description, status, subgoals) and GoalManager class handling all goal operations including persistence, loading at startup, and pruning old completed goals.
Output: goal_manager.py with Goal dataclass and GoalManager class
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
Existing code patterns from context_manager.py and llm_agent.py:
- JSON persistence: `json.dump()`/`json.load()` pattern
- asyncio callback pattern: `asyncio.create_task()` for async callbacks
- dataclass patterns used throughout

From llm_agent.py (lines 395-405):
```python
def add_goal(self, goal: str) -> None:
    """Add an active goal for relevance boosting."""
    self.context_manager.add_goal(goal)

def remove_goal(self, goal: str) -> None:
    """Remove a completed goal."""
    self.context_manager.remove_goal(goal)

def get_active_goals(self) -> List[str]:
    """Get list of active goals."""
    return list(self.context_manager.active_goals)
```

From context_manager.py (lines 114, 256-264):
```python
self.active_goals: List[str] = []

def add_goal(self, goal: str) -> None:
    """Add an active goal for relevance boosting."""
    if goal not in self.active_goals:
        self.active_goals.append(goal)

def remove_goal(self, goal: str) -> None:
    """Remove a completed goal."""
    if goal in self.active_goals:
        self.active_goals.remove(goal)
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Goal dataclass and GoalStatus enum</name>
  <files>goal_manager.py</files>
  <action>
Create goal_manager.py with:

1. **GoalStatus enum** with values: ACTIVE, IN_PROGRESS, COMPLETE, FAILED

2. **Goal dataclass** with fields:
   - `name: str` — user-provided goal text
   - `description: str` — LLM-generated or user-provided description  
   - `status: GoalStatus` — defaults to GoalStatus.ACTIVE
   - `created_at: float` — timestamp from time.time()
   - `subgoals: List[str]` — list of subgoal strings
   - `completed_subgoals: List[int]` — indices of completed subgoals
   - `priority: int` — order in active goals list (default 0)

3. **Methods for serialization**:
   - `to_dict()` — returns dict for JSON serialization
   - `from_dict(d)` — classmethod to reconstruct from dict

4. **Subgoal completion helpers**:
   - `add_subgoal(text: str)` — append to subgoals list
   - `complete_subgoal(index: int)` — add index to completed_subgoals if valid
   - `get_active_subgoal() -> Optional[str]` — returns first non-completed subgoal or None
   - `get_progress() -> tuple[int, int]` — returns (completed_count, total_count)
   - `is_complete() -> bool` — all subgoals completed or no subgoals defined
</action>
  <verify>
python3 -c "
from goal_manager import Goal, GoalStatus
import time

# Test basic creation
g = Goal(name='explore dungeon', description='Find the treasure')
assert g.name == 'explore dungeon'
assert g.status == GoalStatus.ACTIVE
assert g.subgoals == []
assert g.completed_subgoals == []

# Test serialization
d = g.to_dict()
g2 = Goal.from_dict(d)
assert g2.name == g.name
assert g2.status == g.status

# Test subgoals
g.add_subgoal('enter dungeon')
g.add_subgoal('find treasure room')
assert len(g.subgoals) == 2
assert g.get_active_subgoal() == 'enter dungeon'
g.complete_subgoal(0)
assert g.get_progress() == (1, 2)
assert not g.is_complete()
g.complete_subgoal(1)
assert g.is_complete()

print('Goal dataclass tests pass')
"</verify>
  <done>Goal dataclass with status enum, subgoals tracking, and serialization working</done>
</task>

<task type="auto">
  <name>Task 2: Create GoalManager class with CRUD and persistence</name>
  <files>goal_manager.py</files>
  <action>
Extend goal_manager.py with GoalManager class:

1. **Constructor**: `__init__(self, goals_file: str = "goals.json")`
   - Initialize empty goals list
   - Store goals_file path
   - Call `load_goals()`

2. **Core CRUD methods**:
   - `create_goal(name: str, description: str = "") -> Goal` — create new goal, add to list, save
   - `get_goal(goal_id: str) -> Optional[Goal]` — find by ID (use name as ID)
   - `get_active_goals() -> List[Goal]` — filter by ACTIVE or IN_PROGRESS status
   - `update_goal(goal_id: str, **kwargs) -> Optional[Goal]` — update goal fields
   - `delete_goal(goal_id: str) -> bool` — remove goal from list, save
   - `list_goals() -> List[Goal]` — return all goals (active first, then by created_at desc)

3. **Subgoal management**:
   - `add_subgoal(goal_id: str, subgoal: str) -> bool` — add subgoal to goal
   - `complete_subgoal(goal_id: str, index: int) -> bool` — mark subgoal complete

4. **Persistence methods**:
   - `save_goals()` — write all goals to goals.json, prune completed older than 20
   - `load_goals()` — load from goals.json at startup, create empty file if not exists
   - `prune_old_completed(max_keep: int = 20)` — keep only last N completed goals

5. **State callbacks**:
   - `set_on_change_callback(callback)` — callback invoked on any goal state change (for WebSocket broadcast triggering)

6. **ID generation**:
   - Use goal name (lowercase, spaces to underscores) as stable ID
   - Handle duplicate names by appending _1, _2, etc.

Per D-01 (from CONTEXT.md): Goal data includes name, description, status (active/complete/failed), created_at.

Per D-05: Save to goals.json on every state change, keep last 20 completed goals.
</action>
  <verify>
python3 -c "
from goal_manager import GoalManager, Goal, GoalStatus
import os
import tempfile

# Use temp file for testing
with tempfile.NamedTemporaryFile(suffix='.json', delete=False) as f:
    tmp_path = f.name

try:
    gm = GoalManager(goals_file=tmp_path)
    
    # Test create
    g1 = gm.create_goal('explore dungeon', 'Find treasure')
    assert g1.name == 'explore dungeon'
    assert len(gm.list_goals()) == 1
    
    # Test subgoal
    gm.add_subgoal(g1.name, 'enter dungeon')
    gm.add_subgoal(g1.name, 'find vault')
    g1_reloaded = gm.get_goal(g1.name)
    assert len(g1_reloaded.subgoals) == 2
    
    # Test persistence - create new manager with same file
    gm2 = GoalManager(goals_file=tmp_path)
    assert len(gm2.list_goals()) == 1
    
    # Test delete
    gm.delete_goal(g1.name)
    assert len(gm.list_goals()) == 0
    
    print('GoalManager tests pass')
finally:
    os.unlink(tmp_path)
"</verify>
  <done>GoalManager with CRUD, persistence to goals.json, and callback on state changes</done>
</task>

<task type="auto">
  <name>Task 3: Integrate GoalManager with ContextManager</name>
  <files>context_manager.py, llm_agent.py</files>
  <action>
Update existing code to use new GoalManager instead of simple active_goals list:

1. **context_manager.py** changes:
   - Import GoalManager, Goal, GoalStatus from goal_manager
   - Replace `self.active_goals: List[str] = []` with `self.goal_manager: GoalManager = GoalManager()`
   - Update `add_goal()` to call `goal_manager.create_goal()` instead of list append
   - Update `remove_goal()` to call `goal_manager.delete_goal()` instead of list remove
   - Update `get_active_goals()` to call `goal_manager.get_active_goals()`
   - Update `_critical_state["active_goals"]` to return goal names instead of raw list

2. **llm_agent.py** changes:
   - Update `_get_state_for_compaction()` to use `goal_manager.get_active_goals()` for active_goals
   - The `add_goal()`, `remove_goal()`, `get_active_goals()` methods already delegate to context_manager — no change needed there

3. **Preserve existing behavior**:
   - `add_goal(goal: str)` takes just the name string (backward compatible)
   - Internally creates goal with that name and empty description
   - LLM context for goal relevance boosting continues to work (goal names still available)

Note: The GoalManager will be enhanced in later plans to handle subgoal generation and progress evaluation. This plan only establishes the data model and basic persistence.
</action>
  <verify>
python3 -c "
from context_manager import ContextManager
from llm_agent import LLMAgent
import tempfile
import os

# Create temp goals file
with tempfile.NamedTemporaryFile(suffix='.json', delete=False) as f:
    tmp_path = f.name

# Test ContextManager with GoalManager
cm = ContextManager()
cm.goal_manager.goals_file = tmp_path

# Test add_goal (backward compatible interface)
cm.add_goal('explore dungeon')
active = cm.get_active_goals()
assert len(active) == 1
assert active[0].name == 'explore dungeon'

# Test remove_goal
cm.remove_goal('explore dungeon')
assert len(cm.get_active_goals()) == 0

os.unlink(tmp_path)
print('ContextManager integration tests pass')
"</verify>
  <done>ContextManager and LLMAgent integrated with GoalManager, backward compatible</done>
</task>

</tasks>

<verification>
Verify all three tasks complete:
1. Goal dataclass with status enum, subgoals tracking, serialization
2. GoalManager with CRUD, JSON persistence, pruning
3. ContextManager integrated with GoalManager
</verification>

<success_criteria>
- Goals persist across ContextManager instantiations
- `context_manager.add_goal("text")` creates Goal with that name
- `context_manager.get_active_goals()` returns list of Goal objects
- Goals.json contains valid JSON array of goal dicts
- Last 20 completed goals are kept after save
</success_criteria>

<output>
After completion, create `.planning/phases/07-goal-directed-behavior/07-01-SUMMARY.md`
</output>
