---
phase: 07-goal-directed-behavior
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - test_goal_manager.py
  - test_goal_integration.py
autonomous: true
requirements:
  - GOAL-01
  - GOAL-02
  - GOAL-03
  - GOAL-04
  - GOAL-05
  - GOAL-06

must_haves:
  truths:
    - "Unit tests cover Goal dataclass, GoalManager CRUD, and persistence"
    - "Integration tests verify goal lifecycle with MUDClient and LLMAgent"
  artifacts:
    - path: "test_goal_manager.py"
      provides: "Unit tests for goal model and manager"
    - path: "test_goal_integration.py"
      provides: "Integration tests for goal lifecycle"
  key_links:
    - from: "test_goal_manager.py"
      to: "goal_manager.py"
      via: "pytest imports and runs tests"
      pattern: "import.*goal_manager"
    - from: "test_goal_integration.py"
      to: "llm_agent.py"
      via: "tests create LLMAgent and verify goal flow"
      pattern: "import.*llm_agent"
---

<objective>
Create comprehensive unit and integration tests for goal-directed behavior feature.

Purpose: Test the Goal dataclass, GoalManager CRUD operations, persistence, subgoal generation, progress evaluation, and the full goal lifecycle integration with MUDClient and LLMAgent.
Output: test_goal_manager.py (unit tests), test_goal_integration.py (integration tests)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
From existing test patterns in test_llm_agent.py and test_context_manager.py:
```python
import pytest
import asyncio
import tempfile
import os

def test_something():
    # Use tempfile for JSON file tests
    with tempfile.NamedTemporaryFile(suffix='.json', delete=False) as f:
        tmp_path = f.name
    try:
        # test code
    finally:
        os.unlink(tmp_path)
```

From test_context_manager.py:
```python
from context_manager import ContextManager, MemoryEntry, ActivityType
```

Existing test file structure: tests use pytest, async tests use `@pytest.mark.asyncio`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for Goal dataclass and GoalManager</name>
  <files>test_goal_manager.py</files>
  <action>
Create test_goal_manager.py with comprehensive unit tests:

1. **Goal dataclass tests**:
   - test_goal_creation_with_defaults
   - test_goal_creation_with_all_fields
   - test_goal_to_dict_serialization
   - test_goal_from_dict_deserialization
   - test_goal_roundtrip_serialization
   - test_add_subgoal
   - test_complete_subgoal_valid_index
   - test_complete_subgoal_invalid_index_ignored
   - test_get_active_subgoal_returns_first_incomplete
   - test_get_active_subgoal_returns_none_when_all_complete
   - test_get_progress_empty_subgoals
   - test_get_progress_partial_complete
   - test_get_progress_all_complete
   - test_is_complete_with_no_subgoals
   - test_is_complete_with_incomplete_subgoals
   - test_is_complete_with_all_subgoals_complete

2. **GoalManager basic CRUD tests**:
   - test_create_goal_returns_goal_object
   - test_create_goal_adds_to_list
   - test_get_goal_found
   - test_get_goal_not_found
   - test_update_goal_name
   - test_update_goal_description
   - test_update_goal_status
   - test_delete_goal_exists
   - test_delete_goal_not_exists
   - test_list_goals_empty
   - test_list_goals_returns_all
   - test_list_goals_active_first_then_by_created

3. **GoalManager persistence tests**:
   - test_save_goals_creates_json_file
   - test_load_goals_from_file
   - test_load_goals_file_not_exists_creates_empty
   - test_prune_old_completed_keeps_20
   - test_prune_old_completed_preserves_active

4. **GoalManager callback tests**:
   - test_on_change_callback_invoked_on_create
   - test_on_change_callback_invoked_on_update
   - test_on_change_callback_invoked_on_delete

Use tempfile for all file operations. Clean up temp files in finally blocks.
</action>
  <verify>
pytest test_goal_manager.py -v --tb=short 2>&1 | head -80</verify>
  <done>Unit tests for Goal dataclass and GoalManager CRUD/persistence complete</done>
</task>

<task type="auto">
  <name>Task 2: Create integration tests for goal lifecycle</name>
  <files>test_goal_integration.py</files>
  <action>
Create test_goal_integration.py with integration tests:

1. **GoalManager with LLM provider tests**:
   - test_generate_subgoals_calls_provider
   - test_generate_subgoals_updates_goal_subgoals
   - test_generate_subgoals_no_provider_returns_none
   - test_evaluate_progress_calls_provider
   - test_evaluate_progress_updates_completed_subgoals
   - test_advance_subgoal_marks_correct_index
   - test_complete_goal_sets_status_complete
   - test_fail_goal_sets_status_failed

2. **ContextManager integration tests**:
   - test_context_manager_add_goal_creates_goal
   - test_context_manager_remove_goal_deletes_goal
   - test_context_manager_get_active_goals_returns_goals
   - test_context_manager_passes_provider_to_goal_manager

3. **LLMAgent goal integration tests**:
   - test_llm_agent_add_goal_adds_to_manager
   - test_llm_agent_build_prompt_includes_goal_context
   - test_llm_agent_build_prompt_shows_progress
   - test_llm_agent_build_prompt_shows_current_subgoal
   - test_llm_agent_check_and_generate_subgoals_exists
   - test_llm_agent_check_goal_completion_exists
   - test_llm_agent_get_game_state_summary_includes_room

4. **MUDClient goal integration tests**:
   - test_mud_client_has_goal_manager
   - test_mud_client_goal_manager_callback_set
   - test_mud_client_handles_set_goal_command (mock WebSocket)

5. **Full lifecycle test**:
   - test_goal_lifecycle_create_to_complete

Mock LLM provider using a simple "mock" provider that returns controlled responses for testing.
</action>
  <verify>
pytest test_goal_integration.py -v --tb=short 2>&1 | head -80</verify>
  <done>Integration tests for goal lifecycle with ContextManager, LLMAgent, and MUDClient complete</done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite and verify phase requirements</name>
  <files>test_goal_manager.py, test_goal_integration.py</files>
  <action>
Run all Phase 7 related tests together:

1. **Run unit tests**:
   ```
   pytest test_goal_manager.py test_goal_integration.py -v --tb=short
   ```

2. **Verify requirement coverage**:
   - GOAL-01: User can set natural language goals → tests verify set_goal command creates Goal
   - GOAL-02: Agent decomposes into subgoals → tests verify generate_subgoals() method
   - GOAL-03: User can view progress → tests verify get_progress() and build_prompt() output
   - GOAL-04: System detects completion/failure → tests verify evaluate_progress() and status transitions
   - GOAL-05: Goals persist across sessions → tests verify goals.json save/load
   - GOAL-06: Goals influence agent prioritization → tests verify build_prompt() includes goal context

3. **Check test summary**:
   - All tests should pass (or be explicitly skipped with reason)
   - No import errors
   - Clean up any test artifacts
</action>
  <verify>
pytest test_goal_manager.py test_goal_integration.py -v 2>&1 | tail -30</verify>
  <done>Full test suite passes covering all GOAL-01 through GOAL-06 requirements</done>
</task>

</tasks>

<verification>
Verify:
1. test_goal_manager.py: 20+ unit tests covering Goal dataclass and GoalManager CRUD
2. test_goal_integration.py: 15+ integration tests covering lifecycle with other modules
3. All tests pass
4. Requirements GOAL-01 through GOAL-06 covered by tests
</verification>

<success_criteria>
- pytest test_goal_manager.py passes all tests
- pytest test_goal_integration.py passes all tests
- GOAL-01: set_goal creates Goal via WebSocket
- GOAL-02: generate_subgoals() produces 3-5 subgoals
- GOAL-03: get_progress() shows X/Y format
- GOAL-04: status transitions ACTIVE → IN_PROGRESS → COMPLETE/FAILED
- GOAL-05: goals.json persists across instantiations
- GOAL-06: build_prompt() includes goal context for action prioritization
</success_criteria>

<output>
After completion, create `.planning/phases/07-goal-directed-behavior/07-05-SUMMARY.md`
</output>
