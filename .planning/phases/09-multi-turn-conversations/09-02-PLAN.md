---
phase: 09-multi-turn-conversations
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - conversation_manager.py
autonomous: true
requirements:
  - DIALOG-02
  - DIALOG-03

must_haves:
  truths:
    - "Agent classifies each NPC message into a dialogue act type"
    - "Turn history stores act type for each exchange"
    - "LLM classifies dialogue acts when provider available"
  artifacts:
    - path: "conversation_manager.py"
      provides: "LLM-based dialogue act classification integrated into add_turn"
      min_lines: 50
  key_links:
    - from: "conversation_manager.py"
      to: "llm_providers.py"
      via: "provider.chat() for classification"
      pattern: "await.*provider.*chat"
---

<objective>
Implement dialogue act detection using LLM classification. Each NPC message gets classified into one of 5 act types (greeting, question, command, farewell, statement) before being added to conversation turns.
</objective>

<execution_context>
 @$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@conversation_manager.py — From plan 09-01, contains ConversationManager class

# Key interfaces

From llm_providers.py (check exists):
```python
class LLMProvider:
    async def chat(self, messages: List[Dict[str, str]]) -> str:
        """Send a chat request and return the response text."""
```

# Dialogue Act Types (from ConversationManager)
```python
class DialogActType(Enum):
    GREETING = "greeting"      # Hello, hi, good day, etc.
    QUESTION = "question"      # Asking for info
    COMMAND = "command"        # Telling to do something
    FAREWELL = "farewell"      # Goodbye, farewell, until next time
    STATEMENT = "statement"    # Regular statement (default)
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LLM-based dialogue act classification</name>
  <files>conversation_manager.py</files>
  <action>
Extend ConversationManager to support LLM-based dialogue act classification:

1. **Add provider to __init__**:
   - Add `provider: Optional[Any] = None` parameter to __init__
   - Store as self.provider for use in classification

2. **Add classify_dialogue_act method**:
   ```python
   async def classify_dialogue_act(self, text: str) -> DialogActType:
       """Classify dialogue act using LLM.
       
       Returns DialogActType enum value.
       Falls back to STATEMENT if no provider or on error.
       """
   ```

3. **Classification prompt**:
   ```
   Classify the following NPC dialogue into one of these categories:
   - greeting: NPC says hello orwelcomes you
   - question: NPC asks for information
   - command: NPC tells you to do something
   - farewell: NPC says goodbye
   - statement: NPC makes a regular statement
   
   NPC dialogue: "{text}"
   
   Respond with ONLY the category name, nothing else.
   ```

4. **Update add_turn to use async classification**:
   - Make add_turn async: `async def add_turn(...)`
   - If speaker == "npc", await classify_dialogue_act(text) to get act type
   - If no provider, use heuristic fallback (see below)

5. **Heuristic fallback (no LLM)**:
   - Check for greeting keywords: "hello", "hi", "good day", "welcome", "greetings"
   - Check for farewell keywords: "goodbye", "farewell", "see you", "until next time", "come back"
   - Check for question mark in text
   - Check for command keywords: "go", "get", "bring", "kill", "find", "do", "must", "should"
   - Default to STATEMENT

6. **Update callback triggers**:
   - All methods that modify state should trigger callback after save
   - set_on_change_callback for UI updates

Keep backward compatibility: add non-async `add_turn_sync()` that uses STATEMENT for npc turns without classification.
</action>
  <verify>
<automated>cd /Users/burnz/code/llm-mud && python -c "
from conversation_manager import ConversationManager, DialogActType
import asyncio

async def test():
    cm = ConversationManager()
    c = cm.start_conversation('Innkeeper', 'room rental')
    await cm.add_turn('npc', 'Hello traveler! What brings you to my inn?', cm.provider)
    await cm.add_turn('agent', 'I need a room for the night.', cm.provider)
    await cm.add_turn('npc', 'A room costs 10 gold. Do you accept?', cm.provider)
    print(f'Turns: {len(c.turns)}')
    for t in c.turns:
        print(f'  {t.speaker}: {t.act.value}')
asyncio.run(test())
"</automated>
  </verify>
  <done>
ConversationManager.add_turn() is async and classifies NPC dialogue acts using LLM when provider available, falls back to heuristic when not.
</done>
</task>

<task type="auto">
  <name>Task 2: Add turn tracking utilities</name>
  <files>conversation_manager.py</files>
  <action>
Add utilities for tracking conversation progress and topic transitions:

1. **get_turn_count(npc_name: str) -> int**:
   - Return total number of turns for this conversation

2. **get_topic_history(npc_name: str) -> List[Tuple[str, float]]**:
   - Return list of (topic, timestamp) when topic changed
   - Tracked via internal _topic_history: Dict[str, List[Tuple[str, float]]]

3. **get_last_act(npc_name: str) -> Optional[DialogActType]**:
   - Return the DialogActType of the last turn

4. **detect_farewell(npc_name: str) -> bool**:
   - Check if last NPC act is FAREWELL
   - Used by completion handler

5. **get_conversation_summary(npc_name: str) -> str**:
   - Return brief summary: "Conversation with {npc_name} about {topic}, {n} turns"
</action>
  <verify>
<automated>cd /Users/burnz/code/llm-mud && python -c "
from conversation_manager import ConversationManager
cm = ConversationManager()
c = cm.start_conversation('Merchant', 'weapons')
c2 = cm.start_conversation('Innkeeper', 'rooms')
print(f'Active conversations: {len(cm.get_active_conversations())}')
print(f'Total tracked: {len(cm.list_conversations())}')
"</automated>
  </verify>
  <done>
Conversation turn tracking utilities exist: get_turn_count, get_topic_history, get_last_act, detect_farewell, get_conversation_summary.
</done>
</task>

</tasks>

<verification>
1. add_turn() accepts provider and classifies dialogue acts
2. Heuristic fallback works when no provider
3. Turn tracking utilities return correct values
4. Topic history tracks transitions
</verification>

<success_criteria>
- add_turn() is async and uses LLM for classification when provider available
- Falls back to heuristic classification when no provider
- Dialogue acts stored per turn and queryable
- Topic history tracks changes over time
</success_criteria>

<output>
After completion, create `.planning/phases/09-multi-turn-conversations/09-02-SUMMARY.md`
</output>
